# M108 推荐卡片封面展示实现计划

1. 扩展后端推荐响应模型，给 `/api/recommendations` 和 `/api/recommendations/reshuffle` 增加 `cover_url`
2. 补 Python API 测试，锁定 `cover_url` 字段输出
3. 改 popup 推荐卡片 DOM 结构，加入封面区域和占位态
4. 调整 popup 样式，让封面与现有亮色卡片风格一致
5. 补 extension 测试，验证封面和占位渲染
6. 更新 `docs/modules/extension.md`、`docs/modules/recommendation.md`、`docs/changelog.md`、`docs/v0.1-todolist.md`
7. 运行 `ruff check src/ tests/`、`mypy src/`、`pytest -q`、`npm test -- --runInBand`、`npm run typecheck`、`npm run build`
